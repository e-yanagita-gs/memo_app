<% content_for :header_action do %>
  <a class="btn btn-secondary" style="width: 80px;" href="/memos/new">追加</a>
<% end %>

<ul>
  <% @memos.each do |id, post| %>
    <li>
      <a href="/memos/<%= id %>"><%= h(post["title"]) %></a>
    </li>
  <% end %>
</ul>
